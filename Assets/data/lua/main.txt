require("Global");
local socket = require "socket"
main = {}

-- 开始
function start()
	--print(Global.ASSET.AssetBundleLoad(1))
	local asset = Global.ASSET.AssetBundleLoad("data/conf/manifestfile.json");
	--local func = function(path) return GL.ASSET:AssetBundleLoad(path); end
	--local a = func(GL.ASSET.url.."data/conf/manifestfile.json");
	print(asset.error);
	--print("end")
end

-- 更新
function update()
	print(socket.gettime());
end

-- 销毁
function destroy()
	print("destroy");
end

return main