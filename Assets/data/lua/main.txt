require("Global");
local socket = require "socket"
main = {}

-- 开始
function start()
	local ll = CS.Framework.Param.Create();
	print(type(ll));
	
	ll:AddEvent('update', destroy);
	--local c = ll:GetAction("update");
	--print(type(c));
	Global.UI.OpenUI("uiloading", ll);
end

function aa(v)
print(v.GetString('name'));
end

function load(result, obj)
	print("load");
end

-- 更新
function update()
	--print(socket.gettime());
end

-- 销毁
function destroy()
	print("destroy");
end

return main